<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:id="@+id/healthToolItemLayout"
    android:descendantFocusability="blocksDescendants"
    >


    <ImageView
        android:id="@+id/healthToolIcon"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginLeft="20dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/healthToolName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/placeholder"
        android:textSize="22sp"
        android:layout_marginTop="14dp"
        android:layout_marginLeft="16dp"
        android:clickable="false"
        android:focusable="false"
        android:focusableInTouchMode="false"
        android:textColor="#000000"
        app:layout_constraintLeft_toRightOf="@+id/healthToolIcon"
        app:layout_constraintTop_toTopOf="parent"
        />


    <TextView
        android:id="@+id/healthToolDescription"
        android:layout_width="270dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:layout_marginBottom="10dp"
        android:scrollHorizontally="false"
        android:text="@string/placeholder"
        android:textColor="#000000"
        android:textSize="18sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toRightOf="@id/healthToolIcon"
        app:layout_constraintStart_toStartOf="@+id/healthToolName"
        app:layout_constraintTop_toBottomOf="@id/healthToolName" />

</androidx.constraintlayout.widget.ConstraintLayout>
